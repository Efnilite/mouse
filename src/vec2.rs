/// 2d vector with ints
#[derive(Copy, Clone, Debug)]
pub struct Vec2i {
    pub x: u8,
    pub y: u8
}

/// 2d vector with floats
#[derive(Debug)]
pub struct Vec2f {
    pub x: f32,
    pub y: f32
}
